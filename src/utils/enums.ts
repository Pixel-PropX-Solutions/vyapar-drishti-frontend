export enum AuthStates {
    AUTHENTICATED = "AUTHENTICATED",
    INITIALIZING = "INITIALIZING",
    IDLE = "IDLE",
    ERROR = "ERROR",
}

export enum ROLE_ENUM{
    ADMIN="admin",
    CHEMIST="Chemist",
    USER = 'user',
    NULL="null"
}

